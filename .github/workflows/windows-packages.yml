name: Build Windows Packages

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version number (for example: 1.2.3)"
        required: true
        default: "2.6.8"
        type: string
      vintage_story_version:
        description: "Vintage Story server version used to build embedded mods"
        required: true
        default: "1.22.2"
        type: string
  push:
    tags:
      - "v*"

jobs:
  build-windows:
    runs-on: windows-latest

    env:
      PROJECT_PATH: LauncherGo.App/LauncherGo.App.csproj
      EMBEDDED_AUTH_PROJECT: LauncherGo.Services/EmbeddedMods/VsslAuthMod/VsslAuthMod.csproj
      EMBEDDED_REDIRECT_PROJECT: LauncherGo.Services/EmbeddedMods/LauncherGoRedirectMod/LauncherGoRedirectMod.csproj
      EMBEDDED_SERVER_BRIDGE_PROJECT: LauncherGo.Services/EmbeddedMods/LauncherGoServerBridgeMod/LauncherGoServerBridgeMod.csproj
      CONFIGURATION: Release
      RUNTIME: win-x64
      VINTAGE_STORY_VERSION: 1.22.2

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Prepare Vintage Story API refs
        shell: pwsh
        run: |
          $inputVsVersion = "${{ inputs.vintage_story_version }}"
          $vsVersion = if ([string]::IsNullOrWhiteSpace($inputVsVersion)) { $env:VINTAGE_STORY_VERSION } else { $inputVsVersion.Trim() }
          if ([string]::IsNullOrWhiteSpace($vsVersion)) { throw "VINTAGE_STORY_VERSION is empty." }

          $vsRoot = Join-Path $pwd ".vintagestory-runtime"
          $requiredEntries = @(
            "VintagestoryAPI.dll",
            "VintagestoryLib.dll",
            "VintagestoryServer.dll",
            "Lib/protobuf-net.dll",
            "Mods/VSSurvivalMod.dll",
            "Mods/VSEssentials.dll",
            "Lib/Microsoft.Data.Sqlite.dll",
            "Lib/0Harmony.dll",
            "Lib/SkiaSharp.dll"
          )
          $missingReference = $requiredEntries | Where-Object { !(Test-Path -LiteralPath (Join-Path $vsRoot $_) -PathType Leaf) } | Select-Object -First 1
          if ($null -ne $missingReference) {
            New-Item -ItemType Directory -Force -Path $vsRoot | Out-Null
            $zipPath = Join-Path $vsRoot "vs_server_win-x64_$vsVersion.zip"
            $downloadUrl = "https://cdn.vintagestory.at/gamefiles/stable/vs_server_win-x64_$vsVersion.zip"

            Invoke-WebRequest -Uri $downloadUrl -OutFile $zipPath
            Add-Type -AssemblyName System.IO.Compression.FileSystem
            $zip = [System.IO.Compression.ZipFile]::OpenRead($zipPath)
            try {
              foreach ($entryName in $requiredEntries) {
                $entry = $zip.GetEntry($entryName)
                if ($null -eq $entry) {
                  throw "Missing required file '$entryName' in $downloadUrl"
                }

                $targetPath = Join-Path $vsRoot $entryName
                $targetDir = Split-Path -Parent $targetPath
                if (!(Test-Path $targetDir)) {
                  New-Item -ItemType Directory -Force -Path $targetDir | Out-Null
                }

                $entryStream = $entry.Open()
                $fileStream = [System.IO.File]::Open($targetPath, [System.IO.FileMode]::Create, [System.IO.FileAccess]::Write, [System.IO.FileShare]::Read)
                try {
                  $entryStream.CopyTo($fileStream)
                }
                finally {
                  $entryStream.Dispose()
                  $fileStream.Dispose()
                }
              }
            }
            finally {
              $zip.Dispose()
            }

            if (Test-Path $zipPath) {
              Remove-Item -LiteralPath $zipPath -Force
            }
          }

          "VINTAGE_STORY_VERSION=$vsVersion" >> $env:GITHUB_ENV
          "VINTAGE_STORY=$vsRoot" >> $env:GITHUB_ENV
          Write-Host "Using VINTAGE_STORY=$vsRoot"

      - name: Restore
        run: dotnet restore LauncherGo.slnx

      - name: Restore embedded mod projects
        shell: pwsh
        run: |
          dotnet restore $env:EMBEDDED_AUTH_PROJECT
          dotnet restore $env:EMBEDDED_REDIRECT_PROJECT
          dotnet restore $env:EMBEDDED_SERVER_BRIDGE_PROJECT

      - name: Resolve package version
        id: resolve_version
        shell: pwsh
        run: |
          if ("${{ github.event_name }}" -eq "workflow_dispatch") {
            $version = "${{ inputs.version }}"
          }
          elseif ("${{ github.ref_type }}" -eq "tag") {
            $version = "${{ github.ref_name }}"
          }
          else {
            $version = "0.0.0-${{ github.run_number }}"
          }

          if ($version.StartsWith("v")) { $version = $version.Substring(1) }
          if ([string]::IsNullOrWhiteSpace($version)) {
            throw "Resolved version is empty."
          }

          "value=$version" >> $env:GITHUB_OUTPUT

      - name: Build embedded mods
        shell: pwsh
        run: |
          function Build-EmbeddedMod {
            param(
              [string]$Project,
              [string]$DllPath,
              [string]$TargetPath,
              [string]$Name
            )

            dotnet build $Project -c $env:CONFIGURATION --no-restore
            if ($LASTEXITCODE -ne 0) {
              throw "Build failed for embedded $Name mod: $Project"
            }

            if (!(Test-Path $DllPath)) {
              throw "Missing built $Name DLL: $DllPath"
            }

            New-Item -ItemType Directory -Force -Path (Split-Path -Parent $TargetPath) | Out-Null
            Copy-Item -LiteralPath $DllPath -Destination $TargetPath -Force
          }

          Build-EmbeddedMod `
            -Project $env:EMBEDDED_AUTH_PROJECT `
            -DllPath "LauncherGo.Services/EmbeddedMods/VsslAuthMod/bin/$env:CONFIGURATION/net10.0/serverauth.dll" `
            -TargetPath "LauncherGo.Services/EmbeddedMods/launchergoauth/serverauth.dll" `
            -Name "ServerAuth"
          Build-EmbeddedMod `
            -Project $env:EMBEDDED_REDIRECT_PROJECT `
            -DllPath "LauncherGo.Services/EmbeddedMods/LauncherGoRedirectMod/bin/$env:CONFIGURATION/net10.0/launchergoredirect.dll" `
            -TargetPath "LauncherGo.Services/EmbeddedMods/launchergoredirect/launchergoredirect.dll" `
            -Name "Gateway Redirect"
          Build-EmbeddedMod `
            -Project $env:EMBEDDED_SERVER_BRIDGE_PROJECT `
            -DllPath "LauncherGo.Services/EmbeddedMods/LauncherGoServerBridgeMod/bin/$env:CONFIGURATION/net10.0/serverbridge.dll" `
            -TargetPath "LauncherGo.Services/EmbeddedMods/launchergoserverbridge/serverbridge.dll" `
            -Name "Server Bridge"

      - name: Publish installer payload
        run: dotnet publish $env:PROJECT_PATH -c $env:CONFIGURATION -r $env:RUNTIME --self-contained true -p:Version=${{ steps.resolve_version.outputs.value }} -p:InformationalVersion=${{ steps.resolve_version.outputs.value }} -o artifacts/publish/win-x64

      - name: Publish portable single-file
        run: dotnet publish $env:PROJECT_PATH -c $env:CONFIGURATION -r $env:RUNTIME --self-contained true -p:Version=${{ steps.resolve_version.outputs.value }} -p:InformationalVersion=${{ steps.resolve_version.outputs.value }} -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -p:DebugType=None -p:DebugSymbols=false -o artifacts/publish/portable

      - name: Copy .NET runtime license files
        shell: pwsh
        run: |
          $dotnetRoot = Split-Path -Parent (Get-Command dotnet).Source
          foreach ($publishRoot in @("artifacts/publish/win-x64", "artifacts/publish/portable")) {
            Copy-Item -LiteralPath (Join-Path $dotnetRoot "LICENSE.txt") -Destination (Join-Path $publishRoot "LICENSE.txt") -Force
            Copy-Item -LiteralPath (Join-Path $dotnetRoot "ThirdPartyNotices.txt") -Destination (Join-Path $publishRoot "ThirdPartyNotices.txt") -Force
          }

          foreach ($publishRoot in @("artifacts/publish/win-x64", "artifacts/publish/portable")) {
            foreach ($officialAssembly in @("VintagestoryAPI.dll", "VintagestoryLib.dll", "VSSurvivalMod.dll")) {
              if (Get-ChildItem -LiteralPath $publishRoot -Recurse -File -Filter $officialAssembly -ErrorAction SilentlyContinue) {
                throw "Official Vintage Story assembly must not be redistributed: $officialAssembly"
              }
            }
          }

      - name: Copy embedded mod payloads to publish outputs
        shell: pwsh
        run: |
          $mods = @(
            @{ Id = "launchergoauth"; SourceRoot = "LauncherGo.Services/EmbeddedMods/launchergoauth"; Files = @("serverauth.dll", "modinfo.json", "README.txt") },
            @{ Id = "launchergoredirect"; SourceRoot = "LauncherGo.Services/EmbeddedMods/launchergoredirect"; Files = @("launchergoredirect.dll", "modinfo.json", "README.txt") },
            @{ Id = "launchergoserverbridge"; SourceRoot = "LauncherGo.Services/EmbeddedMods/launchergoserverbridge"; Files = @("serverbridge.dll", "modinfo.json", "README.txt") }
          )

          foreach ($mod in $mods) {
            foreach ($fileName in $mod.Files) {
              $sourcePath = Join-Path $mod.SourceRoot $fileName
              if (!(Test-Path $sourcePath)) {
                throw "Missing embedded $($mod.Id) source payload: $sourcePath"
              }
            }

            foreach ($publishRoot in @("artifacts/publish/win-x64", "artifacts/publish/portable")) {
              $targetRoot = Join-Path $publishRoot "EmbeddedMods/$($mod.Id)"
              New-Item -ItemType Directory -Force -Path $targetRoot | Out-Null

              foreach ($fileName in $mod.Files) {
                Copy-Item -LiteralPath (Join-Path $mod.SourceRoot $fileName) -Destination (Join-Path $targetRoot $fileName) -Force
              }
              Copy-Item -LiteralPath "LICENSE" -Destination (Join-Path $targetRoot "LICENSE.txt") -Force
            }
          }

          Set-Content -LiteralPath "artifacts/publish/win-x64/launchergo-package-kind.txt" -Value "Installer" -NoNewline
          Set-Content -LiteralPath "artifacts/publish/portable/launchergo-package-kind.txt" -Value "Portable" -NoNewline

      - name: Verify embedded mod payloads
        shell: pwsh
        run: |
          $requiredPaths = @(
            "artifacts/publish/win-x64/LICENSE",
            "artifacts/publish/win-x64/LICENSE.txt",
            "artifacts/publish/win-x64/NOTICE",
            "artifacts/publish/win-x64/THIRD-PARTY-NOTICES.md",
            "artifacts/publish/win-x64/ThirdPartyNotices.txt",
            "artifacts/publish/win-x64/THIRD-PARTY-LICENSES/README.md",
            "artifacts/publish/win-x64/THIRD-PARTY-LICENSES/MIT.txt",
            "artifacts/publish/win-x64/THIRD-PARTY-LICENSES/Apache-2.0.txt",
            "artifacts/publish/win-x64/THIRD-PARTY-LICENSES/MPL-2.0.txt",
            "artifacts/publish/win-x64/THIRD-PARTY-LICENSES/LGPL-3.0.txt",
            "artifacts/publish/win-x64/THIRD-PARTY-LICENSES/CC-BY-4.0.txt",
            "artifacts/publish/win-x64/LauncherGo.ServerHost.exe",
            "artifacts/publish/win-x64/EmbeddedMods/launchergoauth/serverauth.dll",
            "artifacts/publish/win-x64/EmbeddedMods/launchergoauth/modinfo.json",
            "artifacts/publish/win-x64/EmbeddedMods/launchergoauth/README.txt",
            "artifacts/publish/win-x64/EmbeddedMods/launchergoauth/LICENSE.txt",
            "artifacts/publish/win-x64/EmbeddedMods/launchergoredirect/launchergoredirect.dll",
            "artifacts/publish/win-x64/EmbeddedMods/launchergoredirect/modinfo.json",
            "artifacts/publish/win-x64/EmbeddedMods/launchergoredirect/README.txt",
            "artifacts/publish/win-x64/EmbeddedMods/launchergoredirect/LICENSE.txt",
            "artifacts/publish/win-x64/EmbeddedMods/launchergoserverbridge/serverbridge.dll",
            "artifacts/publish/win-x64/EmbeddedMods/launchergoserverbridge/modinfo.json",
            "artifacts/publish/win-x64/EmbeddedMods/launchergoserverbridge/README.txt",
            "artifacts/publish/win-x64/EmbeddedMods/launchergoserverbridge/LICENSE.txt",
            "artifacts/publish/portable/EmbeddedMods/launchergoauth/serverauth.dll",
            "artifacts/publish/portable/EmbeddedMods/launchergoauth/modinfo.json",
            "artifacts/publish/portable/EmbeddedMods/launchergoauth/README.txt",
            "artifacts/publish/portable/EmbeddedMods/launchergoauth/LICENSE.txt",
            "artifacts/publish/portable/EmbeddedMods/launchergoredirect/launchergoredirect.dll",
            "artifacts/publish/portable/EmbeddedMods/launchergoredirect/modinfo.json",
            "artifacts/publish/portable/EmbeddedMods/launchergoredirect/README.txt",
            "artifacts/publish/portable/EmbeddedMods/launchergoredirect/LICENSE.txt",
            "artifacts/publish/portable/EmbeddedMods/launchergoserverbridge/serverbridge.dll",
            "artifacts/publish/portable/EmbeddedMods/launchergoserverbridge/modinfo.json",
            "artifacts/publish/portable/EmbeddedMods/launchergoserverbridge/README.txt",
            "artifacts/publish/portable/EmbeddedMods/launchergoserverbridge/LICENSE.txt",
            "artifacts/publish/portable/LICENSE",
            "artifacts/publish/portable/LICENSE.txt",
            "artifacts/publish/portable/NOTICE",
            "artifacts/publish/portable/THIRD-PARTY-NOTICES.md",
            "artifacts/publish/portable/ThirdPartyNotices.txt",
            "artifacts/publish/portable/THIRD-PARTY-LICENSES/README.md",
            "artifacts/publish/portable/THIRD-PARTY-LICENSES/MIT.txt",
            "artifacts/publish/portable/THIRD-PARTY-LICENSES/Apache-2.0.txt",
            "artifacts/publish/portable/THIRD-PARTY-LICENSES/MPL-2.0.txt",
            "artifacts/publish/portable/THIRD-PARTY-LICENSES/LGPL-3.0.txt",
            "artifacts/publish/portable/THIRD-PARTY-LICENSES/CC-BY-4.0.txt",
            "artifacts/publish/portable/LauncherGo.ServerHost.exe"
          )

          foreach ($path in $requiredPaths) {
            if (!(Test-Path $path)) {
              throw "Missing embedded mod payload: $path"
            }
          }

      - name: Verify embedded licenses and map ZIP contents
        shell: pwsh
        run: |
          ./scripts/verify-embedded-licenses.ps1 -OutputRoot artifacts/publish/win-x64
          ./scripts/verify-embedded-licenses.ps1 -OutputRoot artifacts/publish/portable

      - name: Prepare embedded mod artifacts
        shell: pwsh
        run: |
          $outputRoot = "artifacts/embedded-mods"
          New-Item -ItemType Directory -Force -Path $outputRoot | Out-Null

          $mods = @(
            @{ Id = "launchergoauth"; Files = @("serverauth.dll", "modinfo.json", "README.txt", "LICENSE.txt") },
            @{ Id = "launchergoredirect"; Files = @("launchergoredirect.dll", "modinfo.json", "README.txt", "LICENSE.txt") },
            @{ Id = "launchergoserverbridge"; Files = @("serverbridge.dll", "modinfo.json", "README.txt", "LICENSE.txt") }
          )

          foreach ($mod in $mods) {
            $modRoot = "artifacts/publish/win-x64/EmbeddedMods/$($mod.Id)"
            $zipPath = Join-Path $outputRoot "$($mod.Id).zip"
            foreach ($fileName in $mod.Files) {
              $requiredPath = Join-Path $modRoot $fileName
              if (!(Test-Path $requiredPath)) {
                throw "Missing embedded $($mod.Id) package file: $requiredPath"
              }
            }

            if (Test-Path $zipPath) {
              Remove-Item -LiteralPath $zipPath -Force
            }

            Compress-Archive -Path (Join-Path $modRoot "*") -DestinationPath $zipPath -CompressionLevel Optimal
          }

      - name: Prepare portable artifact
        shell: pwsh
        run: |
          $version = "${{ steps.resolve_version.outputs.value }}"
          $staging = "artifacts/portable/LauncherGo-portable-$version-win-x64"
          $zipPath = "artifacts/portable/LauncherGo-portable-$version-win-x64.zip"
          $publishRoot = "artifacts/publish/portable"

          if (Test-Path $staging) {
            Remove-Item -LiteralPath $staging -Recurse -Force
          }

          New-Item -ItemType Directory -Force -Path $staging | Out-Null
          Copy-Item -Path (Join-Path $publishRoot "*") -Destination $staging -Recurse -Force

          if (!(Test-Path (Join-Path $staging "LauncherGo.App.exe"))) {
            throw "Missing portable executable."
          }

          if (Test-Path $zipPath) {
            Remove-Item -LiteralPath $zipPath -Force
          }

          Compress-Archive -Path (Join-Path $staging "*") -DestinationPath $zipPath -CompressionLevel Optimal

      - name: Install Inno Setup
        run: choco install innosetup --no-progress -y

      - name: Build installer package
        shell: pwsh
        run: |
          $version = "${{ steps.resolve_version.outputs.value }}"

          New-Item -ItemType Directory -Force -Path artifacts/installer | Out-Null

          $buildDir = (Resolve-Path "artifacts/publish/win-x64").Path
          $outputDir = (Resolve-Path "artifacts/installer").Path

          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" `
            "/DMyAppVersion=$version" `
            "/DBuildDir=$buildDir" `
            "/DOutputDir=$outputDir" `
            "installer/LauncherGo.iss"

      - name: Upload installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: LauncherGo-installer-${{ steps.resolve_version.outputs.value }}-win-x64
          path: artifacts/installer/*.exe
          if-no-files-found: error

      - name: Upload portable artifact
        uses: actions/upload-artifact@v4
        with:
          name: LauncherGo-portable-${{ steps.resolve_version.outputs.value }}-win-x64
          path: artifacts/portable/LauncherGo-portable-${{ steps.resolve_version.outputs.value }}-win-x64.zip
          if-no-files-found: error

      - name: Upload embedded mod artifacts
        uses: actions/upload-artifact@v4
        with:
          name: LauncherGo-embedded-mods-${{ steps.resolve_version.outputs.value }}
          path: artifacts/embedded-mods/*.zip
          if-no-files-found: error
