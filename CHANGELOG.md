# 更新日志

## 2.6.9 - 2026-09-08

### 新增

- 网页管理支持编辑标题、SEO 描述／关键词和 favicon 链接，保存后立即应用；首次返回的 HTML 同步输出 SEO 与 Open Graph 元数据，设置持久化并限制管理员修改。
- 网页右键“添加游戏标记”，支持名称、置顶、已保存名称建议、颜色、原始图标和坐标；本人标记可编辑、删除并同步游戏存档与在线客户端。标记支持快照分享和保存独立副本，删除原标记会撤销分享，隐藏区域限制同样适用。
- 地图搜索新增本人游戏标记（名称、说明、图标名称）、规划路线、出生点及管理员隐藏区域；搜索遵循所有权与隐藏规则，点击路线可完整定位。规划路线在开始绘制前弹窗填写名称与颜色。
- 头像默认由服务器请求玩家客户端当前头部网格与贴图片段，后台生成并缓存 256×256 PNG，无需手动准备图层；重连复用缓存并刷新，传输校验玩家归属、令牌、超时及大小限制。`AvatarAssetsPath` 保留为备用图层模式。
- 隐藏区域弹窗改为单行编辑／移除图标按钮；新增“是否游戏内隐藏”，默认关闭，启用后同步遮挡新版客户端的大地图和小地图，包括管理员；取消后恢复显示，不删除本地地图缓存。
- 网页侧栏“游戏标记”：登录后只读取本人游戏内路径点，保留名称、说明、图标、颜色和 XYZ；管理员也不能借此查看他人的私有标记。
- 右键复制坐标链接；侧栏底部“规划路线”支持左键折线绘制、撤销、命名、配色、编辑、删除及右键分享。分享链接直接预览，其他玩家登录后需主动点击保存，生成自己的独立轨迹副本。
- 显示地图扫描、渲染、重试、排队及等待存档状态和已完成数量；扫描总量未知时不显示虚假百分比。
- 管理员右键框选、编辑和移除隐藏区域，以对角文字标识；“隐藏区域”复选框仅控制管理员预览。普通玩家和访客的各级瓦片、标记、路线、搜索及高度接口均过滤隐藏数据，管理员权限按游戏当前权限校验。

### 修复

- 玩家头像按原生模型的负 X 轴正面取景，移除侧转和俯仰，保留正确左右方向；更新头像缓存标识，旧侧脸头像会重新采集生成。
- 修复多面数角色头像报 `Head texture limit`：将逐面贴图片段打包为有界图集，保留像素、网格和传输大小限制；客户端采集失败、模型超时和打包失败会立即回传状态，不再持续误报“等待客户端采集头部模型”。
- 游戏内隐藏区域遮挡限制在地图对话框深度内，不再盖住添加标记弹窗；管理员网页勾选“隐藏区域”时同步请求抹除后的地图瓦片，取消勾选恢复预览。头像改为截取原生生成完成的头部网格，并支持装备使用的物品／方块图集，补充采集日志与默认头像状态提示。
- 优化地图启停：未变化的模组包跳过部署，Host 与网页资源复用文件指纹，仅重新读取变化的文件；停止时主动取消实时连接和挂起请求。按钮增加启动中／停止中状态与防重复操作，日志记录启停各阶段耗时、缓存命中和强制退出回退。
- 修复 Map Host 转发时自动保存后端 Cookie，可能让不同浏览器请求串用会话的问题；现在只转发当前请求携带的 Cookie。
- 修复部署 ServerMap 后服务器可能长时间停留在 `WorldReady` 的问题：区块事件不再等待地图数据库锁，存档扫描延后到 `RunGame` 后执行。
- 移除区块变化引起的全表索引反复重建，改为按列查询已保存的区块切片，并使用无阻塞重置的有界缓存。
- 修复 ServerMap 配置为禁用时仍启动数据库扫描的问题。
- 修复首次准备 Host 文件与其他启动步骤共用 30 秒超时的问题：改为分阶段限时，Host 缓存准备 120 秒、控制通道 60 秒，启动前后自动化各 10 分钟。
- 增加启动阶段及 Host 缓存细分步骤耗时日志；补齐取消检查，超时后等待未返回的文件操作安全结束再释放启动锁，清理本次创建的 Host 或脚本进程，避免迟到启动与重复启动。
- 启动后自动化失败时明确提示“服务器进程已启动”，不再笼统判为服务器启动失败。

### 优化

- 网页“我的标记”更名为“游戏标记”；地点弹窗采用单行编辑／复制链接图标，传送器两端弹窗支持复制对应坐标链接。
- 领地、地点和路线文字在缩小至 8 方块/像素及更远时自动隐藏，避免挤占地图；仅保留点击选中的标注，取消选中后隐藏，放大后自动恢复。
- 优化领地、地点和路线标注文字：统一常规字体与 13px 字号，细描边替代粗白边；地点标注保留原色并按亮度选择描边，隐藏区域使用浅灰白字与细深边，保持对角缩放和边界裁切。
- 地图新增界面统一深色玻璃按钮与弹窗；缺失图标说明改为信息按钮点击提示，访客隐藏登录后才可用的操作。
- 路线弹窗操作改为单行图标按钮，保留悬停说明与无障碍名称；统一右键菜单字号，将“游戏标记／我的轨迹／隐藏区域”移到玩家列表上方并匹配现有图层勾选框样式。
- 渲染进度精简为红／黄／绿单条进度条（失败／待处理／已完成），点击色段查看统计；零值色段完全隐藏，全部为零时显示可点击查看状态的空轨道。
- 地图笔记侧栏移除列表，仅保留勾选开关；勾选“我的轨迹”同时显示全部已保存路线，分享预览独立显示。
- 规划路线、分享预览与区域选择工具栏按文字和按钮的实际内容宽度收缩，窄屏自动换行，不再横向撑满。
- 路线规划将“撤销一点”替换为“撤销／恢复”，支持连续撤销与恢复轨迹点；无可用历史时禁用按钮，新建轨迹点会清除恢复历史。
- 隐藏区域移除云雾、动画及高亮底块，改用类似领地的左下至右上对角文字：管理员显示区域名称，其他玩家显示“隐藏区域”。文字适配区域大小、随地图缩放并裁切在边界内，不遮住相邻地图，也无空闲动画重绘。
- 隐藏瓦片的 RGBA 数据仍在服务器端清除，相关数据继续过滤；管理员文字预览开关会刷新权限瓦片并记住选择，不改变其他玩家的隐藏规则。
- 地图网页新增手机／触屏布局：菜单、搜索和公告按需展开，适配横竖屏、安全区域和可滚动弹窗，并提供坐标操作及地点标记编辑的触摸入口。
- 地图实时通知合并刷新，复用未变化的图层和玩家标记；拖动及后台页面暂缓刷新，手机玩家轮询间隔调整为 5 秒，并减少瓦片缓存和实时模糊效果。
- 初始地图扫描、手动全图渲染排队、配色重绘排队和保存处理移到后台；存档分页读取，批次之间释放数据库读锁。
- 传送器索引和旧地图缩放瓦片补全延后并限速执行，增加进度日志与停服取消机制，游戏线程不再等待地图任务结束。
- 新增 10 项调度、缓存并发和 SQLite 查询回归测试，并验证 1.22.3 服务端临时新存档及已有测试存档的启动、扫描与停服。
- 新增 13 项启动阶段、取消检查、租约释放及脚本进程清理测试，以及 14 项地图笔记、权限隔离、图标安全和瓦片遮挡测试；补齐头像采集／渲染、启停、SEO 及缓存测试，全量 322 项测试通过。

- 补齐 Feather 图标署名、GPLv3 全文、95 个 NuGet 包的版权清单与原始许可证／第三方声明；发布检查验证许可证覆盖及原始声明校验和，并明确游戏资源缓存的版权归属。

### 注意事项

- 网页标题、SEO 和 favicon 配置需同时更新地图模组、WebRoot 与 Map Host；头像采集需服务端和客户端都更新模组，客户端更新后须完全退出游戏再启动。
- 已部署 ServerMap 的服务器需先停服，再部署新版 `servermap.zip` 后启动；无需删除世界存档或地图缓存。
- 本次标记、轨迹、进度及隐藏区域功能需要同时更新地图模组和 WebRoot；自定义网页目录请执行“手动更新”。游戏标记读写原生路径点，网页规划路线仍单独保存。
- Map Host 的会话隔离修复需要停止地图网页服务，更新 Host 后重新启动。
- 专用服务器不带完整的路径点图标。管理员需安装新版地图模组并进入游戏一次，原始 SVG 自动分批同步并按世界缓存；同步前网页会提示缺少图标。自定义图标需以游戏 SVG 资源提供。
- 隐藏区域始终过滤网页数据；启用“是否游戏内隐藏”后，新版客户端的大地图和小地图也同步遮挡（包括管理员）。不能收回已查看或下载的数据，也不限制游戏内探索。
- 继续提供联网安装版和完整自包含携带版。

## 2.6.8 - 2026-09-08

### 新增

- 集成 ServerMap：网页端、服务端模组和独立 Map Host 统一由 LauncherGo 管理。
- 服务器地图按档案独立配置和部署，支持手动启动、停止、浏览器打开，以及 HTTP/HTTPS 监听。
- HTTPS 支持 `.crt/.pem` 证书与 `.key/.pem` 私钥，启动前校验证书有效期和私钥匹配关系。
- ServerMap 网页资源独立于游戏模组，可通过 Host 反向代理访问模组 API，并保留 Leaflet、WebCartographer 和 VS-LiveMap-Revival 的版权声明。
- 服务器地图移入“管理”菜单，提供档案列表、编辑、部署、启动/停止和证书文件浏览操作。
- 服务器地图新增自定义 `WebRoot` 目录输入、文件夹浏览和“手动更新”按钮；可将内置网页及第三方许可证同步到自定义目录，替换同名文件并保留其他文件。
- QQ 机器人新增 `/tp <设置点名称>` 传送流程及时空齿轮消耗校验。
- Discord 机器人支持 Profile、Guild、Channel 绑定、管理员权限、玩家绑定、自定义 Slash Command 和服务器状态查询。

### 修复

- 修复服务器地图页面因面板挂载在错误页面容器中而显示空白的问题。
- 修复地图网页 `vendor` 下的 Leaflet 和路径规划脚本返回 404，导致 `L is not defined` 的问题。
- 修复地图 Host Release 发布时 RID 输出目录不一致导致的复制失败。
- 修复 ServerHost、GatewayHost 和 ServerMapHost 发布文件及网页资源未完整复制到发布目录的问题。
- 修复更新完成后安装包和 Host 运行时副本持续累积的问题。
- 修复误删 `.runtime/server-host` 中的 EXE 或依赖文件后，完整标记仍导致服务器无法启动的问题；启动时会校验并自动重建全部 Host 文件。
- 修复配置读取反复同步清理 Host 缓存、枚举系统进程，导致空闲拖窗和启动任意服务器后界面持续卡顿的问题。
- 修复并发启动、缺失文件重建和缓存清理之间的竞争；使用中的 Host 目录保持受保护，缺失文件在新目录重建。
- 修复更新缓存清理可能影响下载或安装中的文件；下载与更新脚本通过使用锁交接，解压临时文件纳入受保护的更新目录。
- 修复地图搜索结果显示世界绝对坐标的问题，统一为相对出生点的游戏坐标，保持搜索点击定位正确。
- 修复窄屏地图搜索结果被公告遮挡的问题，调整搜索列表与坐标输入的布局。
- 修复自定义 `WebRoot` 相对路径的配置解析与 Host 读取位置不一致的问题；保存和启动时统一解析为档案 `ServerMap` 目录下的绝对路径。
- 修复内嵌模组复制时遗漏许可证、地图发布时可能复用旧 ZIP 的问题；构建时重新打包地图，发布流程检查 ZIP 内部与独立网页的完整版权文本，部署时拒绝缺少版权文件的地图包。
- 修复两个 Windows 打包工作流未完整提取 ServerMap 所需游戏与第三方 DLL，导致模组编译失败的问题；依赖完整性检查与解压共用九项引用清单，兼容未附带独立 VintagestoryServer.dll 的本地客户端目录。
- 修复发布工作流错误要求 portable 单文件 Host 同时包含 DLL 和依赖清单，导致实际发布成功后仍被判定缺少文件的问题；portable 现在按真实产物校验三个自包含 Host EXE 及其运行时配置。

### 优化

- 安装版主程序保持自包含，ServerHost、GatewayHost 和 ServerMapHost 共用系统 .NET 10 运行时，由联网安装器按需下载安装并校验；portable 包的主程序与三个 Host 均保持完整自包含。
- Host 与更新缓存改为串行后台维护，合并生命周期事件触发的请求，分批删除闲置文件，延迟重试占用项；不保留闲置历史版本，也不增加独立常驻清理进程。
- ServerHost、GatewayHost 和更新程序使用跨进程文件锁保护运行文件，异常退出自动释放；兼容检查旧版 Host，启动器关闭期间的残留在下次启动时补清。
- Host 文件准备移到后台，哈希计算改用固定大小缓冲区，并合并仪表盘每秒重复刷新。
- 网关状态快照写入增加临时文件占用和 I/O 错误重试，并移除后端统计 JSONL 历史快照，避免统计文件无限增长。
- 网关实时带宽改用 2 秒滑动时间窗，状态快照间隔缩短至 500 毫秒，提高短时流量的可见性，流量停止后自动归零。
- 地图定位改为独立 X、Z 输入框，支持负数、小数、回车提交和空值校验，并适配窄屏。
- 自定义地图网页更新在后台逐文件替换，先更新资源再更新首页；同目录更新后刷新浏览器即可，更换目录需要重启地图。
- GitHub Release 工作流移除 small package 和 small installer，仅发布标准安装包、portable 包及嵌入模组包。
- 补齐 ServerMap 模组和网页的作者、版权、MIT、Leaflet BSD-2-Clause、WebCartographer MIT 及 VS-LiveMap-Revival MIT 声明。
- 按实际引用版本修正 LiveMap MIT 原文，明确地图渲染代码、网页图标、路径规划及背景图片的上游来源；独立网页保留完整 LauncherGo MIT 许可证。
- 出生点改用 WebCartographer 随附的原始图标，移除旧时空齿轮图片及构建、发布目录残留，并补充图片来源与打包校验。
- 参考 WebCartographer 将出生点、玩家及传送器图标改为固定屏幕像素尺寸，地点标记和领地文字统一为 10px，避免地图放大或缩小时覆盖层过大。

## 2.6.8-pre.4 - 2026-09-07

### 新增

- QQ 机器人 `/tp <设置点名称>` 现在需要消耗玩家背包中的 1 个时空齿轮（ID `1899`，Code `game:gear-temporal`）。

### 修复

- 传送前通过服务器桥接原子检查并扣除时空齿轮；物品不足、玩家离线或桥接不可用时不会执行传送。

## 2.6.8-pre.3 - 2026-09-06

### 新增

- QQ 机器人新增 `/tp <设置点名称>`：管理员可在 OneBot 页面维护名称与 X/Y/Z 坐标，已绑定玩家可在对应服务器群中传送自己。

### 优化

- 传送指令校验 QQ 玩家绑定与群服务器绑定关系，并限制设置点名称、重复项和坐标范围，避免跨服调用或构造任意服务端指令。

## 2.6.8-pre.2 - 2026-09-06

### 新增

- 新增独立的 Discord 机器人集成，支持 Profile、Guild 与 Channel 绑定、用户或角色管理员权限、自动启动、玩家绑定、自定义 Slash Command、模组清单及模组文件导出。
- Discord 配置页新增“重新部署命令”，可通过 Guild Bulk Overwrite 重置命令，无需清空 Token 或服务器绑定。

### 修复

- Discord `/server status` 和 `/server players` 改为适合聊天阅读的 Markdown 排版，不再直接输出桥接 JSON 结构。
- 更新弹窗保留 `2.6.8-pre.1` 一类预发行后缀，并按 SemVer 正确比较正式版、预发行版和构建元数据。

### 优化

- Discord 命令说明、服务器状态、季节、开关和运行时间根据绑定服务器的 `ServerLanguage` 本地化，覆盖服务端设置中的 22 种语言代码及区域回退。
- Discord 与 OneBot 复用基础命令解析和消息分段能力，同时保持配置、运行状态和玩家绑定数据相互隔离。

## 2.6.8-pre.1 - 2026-09-06

### 优化

- 网关状态快照写入遇到临时文件占用或 I/O 错误时会自动重试，避免实时连接数和流量统计停留在旧值。
- 移除后端统计 JSONL 历史快照，避免统计文件无限增长。
- 调整模组、认证、服务器桥接和自动化页面顶部操作按钮布局与间距。
- 配置编辑页隐藏加载状态和配置路径提示，初始界面作者显示为 `VSCN-Studio Team`。

### 修复

- 修复危险按钮悬浮时因边框尺寸变化导致的整体偏移。

## 2.6.7 - 2026-09-05

### 新增

- QQ 机器人新增 `/bind <游戏玩家名>` 绑定流程：在已绑定服务器的群内发起，游戏内同名玩家发送 QQ 号完成认证；已绑定用户可私聊使用 `/myinfo` 查询实时玩家资料。
- 服务器桥接新增按需 `player.info` 查询，提供单个玩家的相对坐标、生命值、饱食度和背包物品汇总。
- 启动时自动更新检查仅提示正式版；设置页手动检查更新会同时检查预发布版。
- 补充项目 README、开源组件清单、构建测试说明和二进制再分发所需的第三方声明说明。

### 优化

- 玩家坐标转换为游戏中以出生点为原点的显示坐标，不再显示五十万量级的内部绝对坐标。
- `/myinfo` 采用服务器桥接实时查询，显示服务器、连接状态、维度、延迟、游戏模式、在线时间和背包信息，不包含权限详情。
- 项目及第一方嵌入模组许可证改为 MIT；保留 HansJack 作为项目所有者，并同步版权、关于页和发布包许可证副本。

### 修复

- 死亡通知仅使用服务端 Audit 日志产生的最终本地化死亡句子，不再显示 `Gravity` 等内部伤害枚举。
- 时空风暴的接近、即将来临和衰退公告可通过服务器桥接转发到绑定的 QQ 群。

## 2.6.6 - 2026-09-05

### 新增

- 使用本机 NDJSON 服务器桥接替代 OpenServerQuery HTTP 通道，支持状态查询、命令执行和事件订阅。
- QQ 机器人 `/server status` 新增世界时间、季节、TPS、平均 Tick 时间、在线玩家和最近连接事件。
- 玩家死亡事件新增死亡原因、伤害类型和伤害来源信息。
- 仪表盘在线玩家新增延迟和真实加入时间；点击玩家名称可查看 UID、连接状态及启用后的游戏模式、权限组、维度和坐标等扩展信息。

### 优化

- 服务器桥接模组配置支持热加载；启用状态或端口变化时自动重启本机监听器。
- 机器人恢复服务器状态和玩家列表的多行格式，并过滤群聊公告回显。
- 仪表盘人数、玩家名单和峰值统一使用服务器桥接权威快照，不再依赖服务端日志推算；桥接快照补充稳定的加入时间和最近活动时间。
- 机器人 `/server status` 恢复最近聊天记录，并为最近连接事件显示连接状态。

### 修复

- 清理旧桥接模组残留 DLL，避免 Vintage Story 因多个模组程序集而拒绝加载服务器桥接。
- 修复重复的玩家加入事件和 `dotnet watch` 启动时窗口被迁移任务阻塞的问题。

## 2.6.5 - 2026-08-28

### 新增

- QQ 机器人新增 `/modfile` 和 `/modfileall` 群文件指令，分别打包 Universal 模组和当前档案的全部模组。
- 新增项目版权声明和第三方依赖清单；应用、安装包和独立嵌入模组发布包会携带对应许可证文件。

### 优化

- PDF 导出从 QuestPDF 迁移到 MIT 许可证的 PDFsharp/MigraDoc，保留中文字体、自动分页、页眉和页码支持，消除 QuestPDF 社区版或商业版资格风险。
- 统一程序集作者、公司、产品、版权、GPL-3.0-only 和源码仓库元数据，并在安装程序中展示 GPLv3 许可协议和正确的发布者信息。
- 关于页面新增动态版本、安装包类型、版权和许可证信息，并提供源码仓库及随包版权文件入口。

### 注意事项

- PDF 导出仅在运行时读取 Windows 已安装的等线或黑体，不在 LauncherGo 发布包中分发微软字体文件。

## 2.6.4 - 2026-08-28

### 新增

- 模组页面新增“导出”按钮和导出配置弹窗，可选择 CSV、PDF、TXT、Markdown、XLSX 格式及需要导出的表格列。
- 导出列默认不包含配置路径和文件路径，并支持导出模组官网链接、依赖、问题和启用状态等完整信息。
- QQ 机器人新增 `/modslist txt|pdf|md|xlsx|csv` 指令；TXT 直接回复文本，其余格式发送为文件。

### 优化

- 模组官网链接通过官方模组 API 解析，优先使用 `urlalias`，并对数字模组 ID 和网络失败场景提供可靠降级。
- PDF 导出使用系统中文字体嵌入，避免中文模组信息在阅读器中显示乱码。

## 2.6.3 - 2026-08-26

### 新增

- 新增模组更新检查功能，模组表格增加“更新”列和顶部“检查更新”按钮。
- 更新检查结果和最近检查时间会保存到启动器偏好设置，刷新模组列表或重启后仍可查看上次状态。
- 新增模组更新弹窗，显示当前版本、最新版本、发布时间、更新日志和官网链接。
- 支持在更新弹窗中下载官方模组压缩包、校验 `modinfo.json` 并替换旧模组，同时保留模组启用状态。

### 优化

- 更新状态使用“更新”“最新”“检查失败”区分可更新、正常和异常情况，并显示当前档案最近一次检查日期。

### 修复

- 更新失败时尝试恢复原模组文件，避免安装过程中留下不完整的模组文件。

## 2.6.2 - 2026-08-26

### 修复

- 修复模组 ZIP 文件路径包含空格时被错误拆分，导致导入时提示“未检测到包含 modinfo.json 的模组”的问题。
- 模组文件选择器现在统一为路径添加引号，并支持直接粘贴未加引号的单个现存文件路径。

## 2.6.1 - 2026-08-23

### 新增

- 新增多语言界面支持，增加俄语、德语、法语、西班牙语、波兰语和巴西葡萄牙语资源，并统一语言切换和本地化服务。
- 模组导入支持多来源选择、ZIP 多选和引号分隔的多路径输入；最终简化为仅导入包含 `modinfo.json` 的 ZIP 文件。
- 控制台日志过滤支持按关键词搜索、启用开关、匹配模式和本地化显示。
- 自动化支持实例启动前、关服前、关服后等生命周期脚本，可启用 `.bat`/`.cmd` 脚本并管理脚本路径。
- EasyTier 引导/中继节点支持 MVL 兼容的 JSON 订阅链接，并自动解码 `None`、`Base62`、`Base64` 节点地址。
- 模组列表新增名称和运行端显示，读取 `modinfo.json` 的 `name`、`side` 字段。

### 优化

- 优化侧边栏分组和控制台过滤设置布局，过滤表格按内容自适应宽度。
- 稳定本地化 ComboBox 选项初始化，避免语言切换时选项重复或错位。
- 下载版本页面调整搜索框、导入和刷新按钮布局，减少控件间距分散。

### 移除

- 移除 Stratum 服务端相关代码、下载入口和配置项。

## 2.6.0 - 2026-08-20

### 新增

- 新增 2.6.0 双语发布说明，统一记录本版本的功能调整与兼容性变化。

### 修复

- 移除 OSQ 快照、缓存和机器人消费链路中的地图字段，避免继续生成或传播地图数据。

### 优化

- 完整移除 ServerMap/LiveMap 地图模组、部署服务、嵌入资源和相关 UI 配置。
- 清理地图数据导出、存档地图块读取、地图切片生成和地图能力声明，保留服务器状态、玩家、聊天与日志信息上报。

### 注意事项

- 现有档案中的地图模组文件不会再由 LauncherGo 内置或自动部署；如需继续使用，请由管理员自行维护。

## 2.5.9 - 2026-08-16

### 新增

- 新增纯服务端 `LauncherGo Server Bridge` 1.0.1 模组，通过本机认证连接在 Vintage Story 服务端主线程执行命令，绕过 ServerHost 进程 stdin。
- 在“管理 -> 服务器桥接”中提供每档案的部署、启用、测试、令牌轮换、超时、命令长度和 Relay 回退配置。
- 服务器桥接已接入软件控制台、QQ 机器人向游戏发送消息、自动化任务、网关路由与停服命令等统一命令通道。

### 改进

- 服务器桥接令牌支持在线轮换并自动保存；已加载新版桥接模组的服务端无需为此重启。
- Relay 命令转发按单条命令取消与超时处理，stdin 写入失败或阻塞时明确报告命令通道状态，避免后续命令持续堆积。
- LauncherGo 日志尾随过滤重复的 Vintage Story 坐骑位置审计拒绝日志，降低 UI 日志处理压力。

### 修复

- 修复服务器桥接行协议错误地使用多行 JSON，导致服务端无法完整解析请求的问题。
- 修复服务器桥接页面入口与页面归属不一致的问题。
